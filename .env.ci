# Dummy env vars for CI — used for lint, typecheck, and build only.
# These are NOT real credentials. Real secrets are never committed to the repo.

SUPABASE_URL=https://dummy.supabase.co
SUPABASE_ANON_KEY=dummy-anon-key
SUPABASE_DB_URL=postgresql://dummy:dummy@localhost:5432/dummy

OPENAI_API_KEY=sk-dummy

GOOGLE_CLIENT_ID=dummy-client-id
GOOGLE_CLIENT_SECRET=dummy-client-secret
AUTH_SECRET=dummy-auth-secret-for-ci-only
AUTH_URL=http://localhost:3000
